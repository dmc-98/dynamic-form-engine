---
"@dmc--98/dfe-core": minor
---

Expand the template registry for the gallery: add `lead-generation`, `newsletter-signup`, `nps-survey`, `appointment-booking`, and `support-ticket` (16 templates total), plus a `lead-gen` category. A registry-wide test guarantees every template passes `suggestConfigRepairs` with zero errors and loads into the engine, so anything in the gallery is safe to open.
