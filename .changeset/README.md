# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) for versioning and release PR automation.

Typical contributor flow:

```bash
pnpm changeset
```

Typical maintainer flow:

```bash
pnpm release:version
pnpm release
```
