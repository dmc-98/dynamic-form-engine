---
"@dmc--98/dfe-core": minor
---

Add three M1 starter templates — `user-onboarding`, `loan-application`, and `admin-approval-workflow` — each showcasing a core DFE capability (conditional visibility, computed fields, conditional requirement, and step branching). Available via `getTemplate`, `listTemplates`, and `getTemplatesByCategory`.
